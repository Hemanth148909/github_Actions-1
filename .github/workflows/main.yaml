name: JAVA CI PIPELINE

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create new directory
      - run: mkdir hemanth

      - name: create file & cat 
      - run: cd hemanth && touch kumar.txt

      - name: cat file 
      - run: cat kumar.txt
